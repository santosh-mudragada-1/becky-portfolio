# ---------------------------------------------------------------------------
# Public config (exposed to the browser — never put secrets here)
# ---------------------------------------------------------------------------

# Canonical absolute URL of the deployed site (metadata, OG, canonical links).
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# ---------------------------------------------------------------------------
# Server-only secrets (leave blank until the features are wired up)
# ---------------------------------------------------------------------------
# CONTACT_FORM_API_KEY=""
# ANALYTICS_ID=""
