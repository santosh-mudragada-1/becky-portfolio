import { WorkspaceDesk } from "@/components/sections/workspace";

/**
 * Home route = the workspace. A creative Product Manager's desk viewed from
 * above; the objects on it are the navigation into each section.
 */
export default function HomePage() {
  return <WorkspaceDesk />;
}
